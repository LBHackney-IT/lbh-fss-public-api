version: 2.1

orbs:
  aws_assume_role: lbh-hackit/aws_assume_role@0.1.0
  sonarcloud: sonarsource/sonarcloud@2.0.0

executors:
  docker-python:
    docker:
      - image: cimg/python:3.11
  docker-terraform:
    docker:
      - image: hashicorp/terraform:1.14.5
  docker-dotnet:
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:8.0

references:
  workspace_root: &workspace_root "~"
  attach_workspace: &attach_workspace
    attach_workspace:
      at: *workspace_root

commands:
  assume-role-and-persist-workspace:
    description: "Assumes deployment role and persists credentials across jobs"
    parameters:
      aws-account:
        type: string
    steps:
      - checkout
      - aws_assume_role/assume_role:
          account: <<parameters.aws-account>>
          profile_name: default
          role: "LBH_Circle_CI_Deployment_Role"
      - persist_to_workspace:
          root: *workspace_root
          paths:
            - .aws
  deploy-lambda:
    description: "Deploys API via Serverless"
    parameters:
      stage:
        type: enum
        enum: ["development", "staging", "production"]
      aws-central-parameter-account:
        type: enum
        enum: ["AWS_ACCOUNT_DEVELOPMENT", "AWS_ACCOUNT_STAGING", "AWS_ACCOUNT_PRODUCTION"]
    steps:
      - *attach_workspace
      - checkout
      - run:
          name: Install Node
          command: |
            apt-get update
            apt-get install -y curl ca-certificates
            curl -fsSL https://deb.nodesource.com/setup_24.x | bash -
            apt-get install -y nodejs
      - run:
          name: Install AWS CLI
          command: |
            apt-get update
            apt-get install -y awscli
      - run:
          name: Fetch SSM parameters from central account
          command: |
            CENTRAL_ACCOUNT=${<<parameters.aws-central-parameter-account>>}
            export ADDRESSES_URL=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/<<parameters.stage>>-apis/<<parameters.stage>>apis/addresses-api-base-url \
              --query Parameter.Value \
              --output text)
            export FSS_PUBLIC_API_URL=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/<<parameters.stage>>-apis/<<parameters.stage>>apis/fss-public-api-base-url \
              --query Parameter.Value \
              --output text)
            export HOST=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-hostname \
              --query Parameter.Value \
              --output text)
            export PORT=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-port \
              --query Parameter.Value \
              --output text)
            export DATABASE=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-database \
              --query Parameter.Value \
              --output text)
            export IMAGE_BUCKET_NAME=$(aws ssm get-parameter \
              --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-common-api-<<parameters.stage>>/fss-common-api/images-bucket-name \
              --query Parameter.Value \
              --output text)

            echo "export ADDRESSES_URL=${ADDRESSES_URL}" >> $BASH_ENV
            echo "export HOST=${HOST}" >> $BASH_ENV
            echo "export PORT=${PORT}" >> $BASH_ENV
            echo "export DATABASE=${DATABASE}" >> $BASH_ENV
            echo "export IMAGE_BUCKET_NAME=${IMAGE_BUCKET_NAME}" >> $BASH_ENV
            echo "export FSS_PUBLIC_API_URL=${FSS_PUBLIC_API_URL}" >> $BASH_ENV

      - run:
          name: Build and deploy lambda
          working_directory: ./LBHFSSPublicAPI/
          command: |
            set -e
            chmod +x ./build.sh
            ./build.sh
            npx --yes serverless@4 deploy \
              --stage <<parameters.stage>> \
              --param="addressUrl=${ADDRESSES_URL}" \
              --param="dbHost=${HOST}" \
              --param="dbPort=${PORT}" \
              --param="dbDatabase=${DATABASE}" \
              --param="imageBucketName=${IMAGE_BUCKET_NAME}" \
              --param="fssPublicApiUrl=${FSS_PUBLIC_API_URL}" \
              --conceal

  migrate-database:
    description: "Migrate database"
    parameters:
      stage:
        type: enum
        enum: ["development", "staging", "production"]
      aws-central-parameter-account:
        type: enum
        enum: ["AWS_ACCOUNT_DEVELOPMENT", "AWS_ACCOUNT_STAGING", "AWS_ACCOUNT_PRODUCTION"]
    steps:
      - *attach_workspace
      - checkout
      - run:
          name: Install dependencies
          command: |
            apt-get update
            apt-get install -y unzip openssh-client curl awscli
            curl "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/ubuntu_64bit/session-manager-plugin.deb" -o "session-manager-plugin.deb"
            dpkg -i session-manager-plugin.deb
      - run:
          name: Install dotnet ef core
          command: dotnet tool install dotnet-ef --tool-path ./dotnet-ef-local/
      - run:
          name: Restore dotnet packages
          command: dotnet restore ./LBHFSSPublicAPI/LBHFSSPublicAPI.csproj
      - run:
          name: SSH into RDS and migrate database
          command: |
            CENTRAL_ACCOUNT=${<<parameters.aws-central-parameter-account>>}
            aws ssm get-parameter --name "/platform-apis-jump-box-pem-key" --output text --query Parameter.Value > ./private-key.pem
            chmod 400 ./private-key.pem
            HOST=$(aws ssm get-parameter --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-hostname --query Parameter.Value --output text)
            PORT=$(aws ssm get-parameter --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-port --query Parameter.Value --output text)
            INSTANCE_NAME=$(aws ssm get-parameter --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/<<parameters.stage>>-apis/<<parameters.stage>>apis/jump-box-instance-name --query Parameter.Value --output text)
            ssh -4 -i ./private-key.pem -Nf -M -L 6000:${HOST}:${PORT} -o "UserKnownHostsFile=/dev/null" -o "StrictHostKeyChecking=no" -o ProxyCommand="aws ssm start-session --target %h --document AWS-StartSSHSession --parameters portNumber=%p --region=eu-west-2" ec2-user@${INSTANCE_NAME}
            PASSWORD=$(aws ssm get-parameter --name /fss-public-api/<<parameters.stage>>/postgres-password --query Parameter.Value --output text)
            USERNAME=$(aws ssm get-parameter --name /fss-public-api/<<parameters.stage>>/postgres-username --query Parameter.Value --output text)
            DATABASE=$(aws ssm get-parameter --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/fss-public-api-<<parameters.stage>>/fss-public-api/postgres-database --query Parameter.Value --output text)
            ADDRESSES_URL=$(aws ssm get-parameter --name arn:aws:ssm:eu-west-2:${CENTRAL_ACCOUNT}:parameter/<<parameters.stage>>apis/<<parameters.stage>>-apis/<<parameters.stage>>apis/addresses-api-base-url --query Parameter.Value --output text)
            ADDRESSES_KEY=$(aws ssm get-parameter --name /fss-public-api/<<parameters.stage>>/addresses-api-key --query Parameter.Value --output text)
            ADDRESSES_API_TOKEN=$(aws ssm get-parameter --name /fss-common-api/<<parameters.stage>>/addresses-api-token --query Parameter.Value --output text)
            CONN_STR="Host=localhost;Password=${PASSWORD};Port=6000;Username=${USERNAME};Database=${DATABASE}"
            cd ./LBHFSSPublicAPI/
            ADDRESSES_API_BASE_URL=${ADDRESSES_URL} ADDRESSES_API_KEY=${ADDRESSES_KEY} ADDRESSES_API_TOKEN=${ADDRESSES_API_TOKEN} CONNECTION_STRING=${CONN_STR} ./../dotnet-ef-local/dotnet-ef database update

  preview-terraform:
    description: "Previews terraform configuration changes"
    parameters:
      environment:
        type: string
    steps:
      - *attach_workspace
      - checkout
      - run:
          name: preview terraform
          command: |
            terraform init \
              -backend-config="bucket=terraform-state-<<parameters.environment>>-apis" \
              -backend-config="key=services/fss-public-api/terraform.tfstate"
            terraform validate
            terraform plan -var="environment=<<parameters.environment>>"

  deploy-terraform:
    description: "Initializes and applies terraform configuration"
    parameters:
      environment:
        type: string
    steps:
      - *attach_workspace
      - checkout
      - run:
          name: Initialize and apply terraform
          command: |
            terraform init \
              -backend-config="bucket=terraform-state-<<parameters.environment>>-apis" \
              -backend-config="key=services/fss-public-api/terraform.tfstate"
            terraform validate
            terraform apply -auto-approve -var="environment=<<parameters.environment>>"
      - run:
          name: show terraform outputs
          command: terraform output -json
          when: on_success
      - store_artifacts:
          path: /tmp/terraform-outputs-<<parameters.environment>>.json
          destination: terraform-outputs

jobs:
  check-code-formatting:
    executor: docker-dotnet
    steps:
      - checkout
      - run:
          name: Install dotnet format
          command: dotnet tool install dotnet-format --tool-path ./dotnet-format-local/
      - run:
          name: Run formatter check
          command: ./dotnet-format-local/dotnet-format --check
  sonar-scan:
    executor: docker-python
    steps:
      - checkout:
          method: full
      - attach_workspace:
          at: .
      - sonarcloud/scan
  build-and-test:
    executor: docker-python
    steps:
      - checkout
      - setup_remote_docker
      - run:
            name: Start database
            command: |
              docker compose up -d test-database
              echo "Waiting for database to be ready..."
              timeout 60 sh -c 'until docker compose exec -T test-database pg_isready -U postgres; do sleep 2; done'
      - run:
          name: Build test image
          command: docker compose build fss-public-api-test
      - run:
          name: Run tests with coverage (Coverlet OpenCover for SonarCloud)
          command: |
            mkdir -p coverage
            docker compose run --rm \
              -v "$(pwd)/coverage:/app/coverage" \
              -e ADDRESSES_API_BASE_URL -e ADDRESSES_API_KEY -e ADDRESSES_API_TOKEN \
              fss-public-api-test
      - run:
          name: Cleanup
          when: always
          command: docker compose down -v
      - persist_to_workspace:
          root: .
          paths:
            - coverage
  assume-role-development:
    executor: docker-python
    steps:
      - assume-role-and-persist-workspace:
          aws-account: $AWS_ACCOUNT_DEVELOPMENT
  assume-role-staging:
    executor: docker-python
    steps:
      - assume-role-and-persist-workspace:
          aws-account: $AWS_ACCOUNT_STAGING
  assume-role-production:
    executor: docker-python
    steps:
      - assume-role-and-persist-workspace:
          aws-account: $AWS_ACCOUNT_PRODUCTION
  deploy-to-development:
    executor: docker-dotnet
    steps:
      - deploy-lambda:
          stage: "development"
          aws-central-parameter-account: "AWS_ACCOUNT_DEVELOPMENT"
  deploy-to-staging:
    executor: docker-dotnet
    steps:
      - deploy-lambda:
          stage: "staging"
          aws-central-parameter-account: "AWS_ACCOUNT_STAGING"
  deploy-to-production:
    executor: docker-dotnet
    steps:
      - deploy-lambda:
          stage: "production"
          aws-central-parameter-account: "AWS_ACCOUNT_PRODUCTION"
  migrate-database-development:
    executor: docker-dotnet
    steps:
      - migrate-database:
          stage: "development"
          aws-central-parameter-account: "AWS_ACCOUNT_DEVELOPMENT"
  migrate-database-staging:
    executor: docker-dotnet
    steps:
      - migrate-database:
          stage: "staging"
          aws-central-parameter-account: "AWS_ACCOUNT_STAGING"
  migrate-database-production:
    executor: docker-dotnet
    steps:
      - migrate-database:
          stage: "production"
          aws-central-parameter-account: "AWS_ACCOUNT_PRODUCTION"
  deploy-development-terraform:
    executor: docker-terraform
    steps:
      - deploy-terraform:
          environment: "development"
  deploy-staging-terraform:
    executor: docker-terraform
    steps:
      - deploy-terraform:
          environment: "staging"
  deploy-production-terraform:
    executor: docker-terraform
    steps:
      - deploy-terraform:
          environment: "production"
  preview-development-terraform:
    executor: docker-terraform
    steps:
      - preview-terraform:
          environment: "development"
  preview-staging-terraform:
    executor: docker-terraform
    steps:
      - preview-terraform:
          environment: "staging"
  preview-production-terraform:
    executor: docker-terraform
    steps:
      - preview-terraform:
          environment: "production"

workflows:
  feature:
    jobs:
      - check-code-formatting:
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - build-and-test:
          context: find-support-services-public-api-context
          requires: [check-code-formatting]
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - sonar-scan:
          context: SonarCloud
          requires: [build-and-test]
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - assume-role-development:
          context: api-assume-role-development-context
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - preview-development-terraform:
          requires:
            - assume-role-development
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - assume-role-staging:
          context: api-assume-role-staging-context
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - preview-staging-terraform:
          requires:
            - assume-role-staging
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - assume-role-production:
          context: api-assume-role-production-context
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
      - preview-production-terraform:
          requires:
            - assume-role-production
          filters:
            branches:
              ignore:
                - develop
                - master
                - feature/add-presigned-s3
  check-and-deploy-development:
    jobs:
      - check-code-formatting:
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - build-and-test:
          context: find-support-services-public-api-context
          requires: [check-code-formatting]
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - sonar-scan:
          context: SonarCloud
          requires: [build-and-test]
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - permit-development-release:
          type: approval
          requires:
            - build-and-test
            - sonar-scan
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - assume-role-development:
          context: api-assume-role-development-context
          requires:
            - permit-development-release
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - migrate-database-development:
          context: central-parameter-store-context
          requires:
            - assume-role-development
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - deploy-to-development:
          context:
            - "Serverless Framework"
            - central-parameter-store-context
          requires: [assume-role-development, migrate-database-development]
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
  check-and-deploy-staging-and-production:
    jobs:
      - build-and-test:
          context: find-support-services-public-api-context
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - permit-staging-release:
          type: approval
          requires:
            - build-and-test
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - assume-role-staging:
          context: api-assume-role-staging-context
          requires:
            - permit-staging-release
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - migrate-database-staging:
          context: central-parameter-store-context
          requires:
            - assume-role-staging
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - deploy-to-staging:
          context:
            - "Serverless Framework"
            - central-parameter-store-context
          requires: [assume-role-staging, migrate-database-staging]
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - permit-production-release:
          type: approval
          requires:
            - deploy-to-staging
          filters:
            branches:
              only: master
      - assume-role-production:
          context: api-assume-role-production-context
          requires:
            - permit-production-release
          filters:
            branches:
              only: master
      - migrate-database-production:
          context: central-parameter-store-context
          requires:
            - assume-role-production
          filters:
            branches:
              only: master
      - deploy-to-production:
          context:
            - "Serverless Framework"
            - central-parameter-store-context
          requires: [assume-role-production, migrate-database-production]
          filters:
            branches:
              only: master
  terraform-release:
    jobs:
      - assume-role-development:
          context: api-assume-role-development-context
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - preview-development-terraform:
          requires:
            - assume-role-development
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - permit-terraform-development-release:
          type: approval
          requires:
            - preview-development-terraform
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - deploy-development-terraform:
          requires:
            - permit-terraform-development-release
          filters:
            branches:
              only: [develop, feature/add-presigned-s3]
      - assume-role-staging:
          context: api-assume-role-staging-context
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - preview-staging-terraform:
          requires:
            - assume-role-staging
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - permit-terraform-staging-release:
          type: approval
          requires:
            - preview-staging-terraform
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - deploy-staging-terraform:
          requires:
            - permit-terraform-staging-release
          filters:
            branches:
              only: [master, feature/add-presigned-s3]
      - assume-role-production:
          context: api-assume-role-production-context
          requires:
            - deploy-staging-terraform
          filters:
            branches:
              only: master
      - preview-production-terraform:
          requires:
            - assume-role-production
          filters:
            branches:
              only: master
      - permit-terraform-production-release:
          type: approval
          requires:
            - preview-production-terraform
          filters:
            branches:
              only: master
      - deploy-production-terraform:
          requires:
            - permit-terraform-production-release
          filters:
            branches:
              only: master
